{
  "name": "mifal-israel-monorepo",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@11.1.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "auto-commit": "pnpm --filter @mifal-israel/scripts run start"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^6.0.3",
    "prettier": "^3.0.0",
    "eslint": "^10.5.0"
  },
  "onlyBuiltDependencies": [
    "@prisma/client",
    "@prisma/engines",
    "prisma",
    "unrs-resolver"
  ]
}
